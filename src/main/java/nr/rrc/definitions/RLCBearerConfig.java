/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class RLCBearerConfig implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static class ServedRadioBearer implements BerType, Serializable {

		private static final long serialVersionUID = 1L;

		public byte[] code = null;
		private SRBIdentity srbIdentity = null;
		private DRBIdentity drbIdentity = null;
		
		public ServedRadioBearer() {
		}

		public ServedRadioBearer(byte[] code) {
			this.code = code;
		}

		public void setSrbIdentity(SRBIdentity srbIdentity) {
			this.srbIdentity = srbIdentity;
		}

		public SRBIdentity getSrbIdentity() {
			return srbIdentity;
		}

		public void setDrbIdentity(DRBIdentity drbIdentity) {
			this.drbIdentity = drbIdentity;
		}

		public DRBIdentity getDrbIdentity() {
			return drbIdentity;
		}

		public int encode(OutputStream reverseOS) throws IOException {

			if (code != null) {
				for (int i = code.length - 1; i >= 0; i--) {
					reverseOS.write(code[i]);
				}
				return code.length;
			}

			int codeLength = 0;
			if (drbIdentity != null) {
				codeLength += drbIdentity.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 1
				reverseOS.write(0x81);
				codeLength += 1;
				return codeLength;
			}
			
			if (srbIdentity != null) {
				codeLength += srbIdentity.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 0
				reverseOS.write(0x80);
				codeLength += 1;
				return codeLength;
			}
			
			throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
		}

		public int decode(InputStream is) throws IOException {
			return decode(is, null);
		}

		public int decode(InputStream is, BerTag berTag) throws IOException {

			int codeLength = 0;
			BerTag passedTag = berTag;

			if (berTag == null) {
				berTag = new BerTag();
				codeLength += berTag.decode(is);
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
				srbIdentity = new SRBIdentity();
				codeLength += srbIdentity.decode(is, false);
				return codeLength;
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
				drbIdentity = new DRBIdentity();
				codeLength += drbIdentity.decode(is, false);
				return codeLength;
			}

			if (passedTag != null) {
				return 0;
			}

			throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
		}

		public void encodeAndSave(int encodingSizeGuess) throws IOException {
			ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
			encode(reverseOS);
			code = reverseOS.getArray();
		}

		public String toString() {
			StringBuilder sb = new StringBuilder();
			appendAsString(sb, 0);
			return sb.toString();
		}

		public void appendAsString(StringBuilder sb, int indentLevel) {

			if (srbIdentity != null) {
				sb.append("srbIdentity: ").append(srbIdentity);
				return;
			}

			if (drbIdentity != null) {
				sb.append("drbIdentity: ").append(drbIdentity);
				return;
			}

			sb.append("<none>");
		}

	}

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private LogicalChannelIdentity logicalChannelIdentity = null;
	private ServedRadioBearer servedRadioBearer = null;
	private BerEnum reestablishRLC = null;
	private RLCConfig rlcConfig = null;
	private LogicalChannelConfig macLogicalChannelConfig = null;
	
	public RLCBearerConfig() {
	}

	public RLCBearerConfig(byte[] code) {
		this.code = code;
	}

	public void setLogicalChannelIdentity(LogicalChannelIdentity logicalChannelIdentity) {
		this.logicalChannelIdentity = logicalChannelIdentity;
	}

	public LogicalChannelIdentity getLogicalChannelIdentity() {
		return logicalChannelIdentity;
	}

	public void setServedRadioBearer(ServedRadioBearer servedRadioBearer) {
		this.servedRadioBearer = servedRadioBearer;
	}

	public ServedRadioBearer getServedRadioBearer() {
		return servedRadioBearer;
	}

	public void setReestablishRLC(BerEnum reestablishRLC) {
		this.reestablishRLC = reestablishRLC;
	}

	public BerEnum getReestablishRLC() {
		return reestablishRLC;
	}

	public void setRlcConfig(RLCConfig rlcConfig) {
		this.rlcConfig = rlcConfig;
	}

	public RLCConfig getRlcConfig() {
		return rlcConfig;
	}

	public void setMacLogicalChannelConfig(LogicalChannelConfig macLogicalChannelConfig) {
		this.macLogicalChannelConfig = macLogicalChannelConfig;
	}

	public LogicalChannelConfig getMacLogicalChannelConfig() {
		return macLogicalChannelConfig;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		int sublength;

		if (macLogicalChannelConfig != null) {
			codeLength += macLogicalChannelConfig.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 4
			reverseOS.write(0xA4);
			codeLength += 1;
		}
		
		if (rlcConfig != null) {
			sublength = rlcConfig.encode(reverseOS);
			codeLength += sublength;
			codeLength += BerLength.encodeLength(reverseOS, sublength);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 3
			reverseOS.write(0xA3);
			codeLength += 1;
		}
		
		if (reestablishRLC != null) {
			codeLength += reestablishRLC.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (servedRadioBearer != null) {
			sublength = servedRadioBearer.encode(reverseOS);
			codeLength += sublength;
			codeLength += BerLength.encodeLength(reverseOS, sublength);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
			reverseOS.write(0xA1);
			codeLength += 1;
		}
		
		codeLength += logicalChannelIdentity.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, PRIMITIVE, 0
		reverseOS.write(0x80);
		codeLength += 1;
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			logicalChannelIdentity = new LogicalChannelIdentity();
			subCodeLength += logicalChannelIdentity.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		else {
			throw new IOException("Tag does not match the mandatory sequence element tag.");
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
			subCodeLength += length.decode(is);
			servedRadioBearer = new ServedRadioBearer();
			subCodeLength += servedRadioBearer.decode(is, null);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			reestablishRLC = new BerEnum();
			subCodeLength += reestablishRLC.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 3)) {
			subCodeLength += length.decode(is);
			rlcConfig = new RLCConfig();
			subCodeLength += rlcConfig.decode(is, null);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 4)) {
			macLogicalChannelConfig = new LogicalChannelConfig();
			subCodeLength += macLogicalChannelConfig.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		sb.append("\n");
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (logicalChannelIdentity != null) {
			sb.append("logicalChannelIdentity: ").append(logicalChannelIdentity);
		}
		else {
			sb.append("logicalChannelIdentity: <empty-required-field>");
		}
		
		if (servedRadioBearer != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("servedRadioBearer: ");
			servedRadioBearer.appendAsString(sb, indentLevel + 1);
		}
		
		if (reestablishRLC != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("reestablishRLC: ").append(reestablishRLC);
		}
		
		if (rlcConfig != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("rlcConfig: ");
			rlcConfig.appendAsString(sb, indentLevel + 1);
		}
		
		if (macLogicalChannelConfig != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("macLogicalChannelConfig: ");
			macLogicalChannelConfig.appendAsString(sb, indentLevel + 1);
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

