/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class CAParametersEUTRA implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerEnum multipleTimingAdvance = null;
	private BerEnum simultaneousRxTx = null;
	private BerBitString supportedNAICS2CRSAP = null;
	private BerEnum additionalRxTxPerformanceReq = null;
	private BerEnum ueCAPowerClassN = null;
	
	public CAParametersEUTRA() {
	}

	public CAParametersEUTRA(byte[] code) {
		this.code = code;
	}

	public void setMultipleTimingAdvance(BerEnum multipleTimingAdvance) {
		this.multipleTimingAdvance = multipleTimingAdvance;
	}

	public BerEnum getMultipleTimingAdvance() {
		return multipleTimingAdvance;
	}

	public void setSimultaneousRxTx(BerEnum simultaneousRxTx) {
		this.simultaneousRxTx = simultaneousRxTx;
	}

	public BerEnum getSimultaneousRxTx() {
		return simultaneousRxTx;
	}

	public void setSupportedNAICS2CRSAP(BerBitString supportedNAICS2CRSAP) {
		this.supportedNAICS2CRSAP = supportedNAICS2CRSAP;
	}

	public BerBitString getSupportedNAICS2CRSAP() {
		return supportedNAICS2CRSAP;
	}

	public void setAdditionalRxTxPerformanceReq(BerEnum additionalRxTxPerformanceReq) {
		this.additionalRxTxPerformanceReq = additionalRxTxPerformanceReq;
	}

	public BerEnum getAdditionalRxTxPerformanceReq() {
		return additionalRxTxPerformanceReq;
	}

	public void setUeCAPowerClassN(BerEnum ueCAPowerClassN) {
		this.ueCAPowerClassN = ueCAPowerClassN;
	}

	public BerEnum getUeCAPowerClassN() {
		return ueCAPowerClassN;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (ueCAPowerClassN != null) {
			codeLength += ueCAPowerClassN.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 4
			reverseOS.write(0x84);
			codeLength += 1;
		}
		
		if (additionalRxTxPerformanceReq != null) {
			codeLength += additionalRxTxPerformanceReq.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (supportedNAICS2CRSAP != null) {
			codeLength += supportedNAICS2CRSAP.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (simultaneousRxTx != null) {
			codeLength += simultaneousRxTx.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			reverseOS.write(0x81);
			codeLength += 1;
		}
		
		if (multipleTimingAdvance != null) {
			codeLength += multipleTimingAdvance.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			multipleTimingAdvance = new BerEnum();
			subCodeLength += multipleTimingAdvance.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			simultaneousRxTx = new BerEnum();
			subCodeLength += simultaneousRxTx.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			supportedNAICS2CRSAP = new BerBitString();
			subCodeLength += supportedNAICS2CRSAP.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			additionalRxTxPerformanceReq = new BerEnum();
			subCodeLength += additionalRxTxPerformanceReq.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 4)) {
			ueCAPowerClassN = new BerEnum();
			subCodeLength += ueCAPowerClassN.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (multipleTimingAdvance != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("multipleTimingAdvance: ").append(multipleTimingAdvance);
			firstSelectedElement = false;
		}
		
		if (simultaneousRxTx != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("simultaneousRxTx: ").append(simultaneousRxTx);
			firstSelectedElement = false;
		}
		
		if (supportedNAICS2CRSAP != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("supportedNAICS2CRSAP: ").append(supportedNAICS2CRSAP);
			firstSelectedElement = false;
		}
		
		if (additionalRxTxPerformanceReq != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("additionalRxTxPerformanceReq: ").append(additionalRxTxPerformanceReq);
			firstSelectedElement = false;
		}
		
		if (ueCAPowerClassN != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ueCAPowerClassN: ").append(ueCAPowerClassN);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

