/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class PhyParametersFR1 implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerEnum pdcchMonitoringSingleOccasion = null;
	private BerEnum scs60kHz = null;
	private BerEnum pdsch256QAMFR1 = null;
	private BerEnum pdschREMappingFR1 = null;
	
	public PhyParametersFR1() {
	}

	public PhyParametersFR1(byte[] code) {
		this.code = code;
	}

	public void setPdcchMonitoringSingleOccasion(BerEnum pdcchMonitoringSingleOccasion) {
		this.pdcchMonitoringSingleOccasion = pdcchMonitoringSingleOccasion;
	}

	public BerEnum getPdcchMonitoringSingleOccasion() {
		return pdcchMonitoringSingleOccasion;
	}

	public void setScs60kHz(BerEnum scs60kHz) {
		this.scs60kHz = scs60kHz;
	}

	public BerEnum getScs60kHz() {
		return scs60kHz;
	}

	public void setPdsch256QAMFR1(BerEnum pdsch256QAMFR1) {
		this.pdsch256QAMFR1 = pdsch256QAMFR1;
	}

	public BerEnum getPdsch256QAMFR1() {
		return pdsch256QAMFR1;
	}

	public void setPdschREMappingFR1(BerEnum pdschREMappingFR1) {
		this.pdschREMappingFR1 = pdschREMappingFR1;
	}

	public BerEnum getPdschREMappingFR1() {
		return pdschREMappingFR1;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (pdschREMappingFR1 != null) {
			codeLength += pdschREMappingFR1.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (pdsch256QAMFR1 != null) {
			codeLength += pdsch256QAMFR1.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (scs60kHz != null) {
			codeLength += scs60kHz.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			reverseOS.write(0x81);
			codeLength += 1;
		}
		
		if (pdcchMonitoringSingleOccasion != null) {
			codeLength += pdcchMonitoringSingleOccasion.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			pdcchMonitoringSingleOccasion = new BerEnum();
			subCodeLength += pdcchMonitoringSingleOccasion.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			scs60kHz = new BerEnum();
			subCodeLength += scs60kHz.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			pdsch256QAMFR1 = new BerEnum();
			subCodeLength += pdsch256QAMFR1.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			pdschREMappingFR1 = new BerEnum();
			subCodeLength += pdschREMappingFR1.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (pdcchMonitoringSingleOccasion != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("pdcchMonitoringSingleOccasion: ").append(pdcchMonitoringSingleOccasion);
			firstSelectedElement = false;
		}
		
		if (scs60kHz != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("scs60kHz: ").append(scs60kHz);
			firstSelectedElement = false;
		}
		
		if (pdsch256QAMFR1 != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("pdsch256QAMFR1: ").append(pdsch256QAMFR1);
			firstSelectedElement = false;
		}
		
		if (pdschREMappingFR1 != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("pdschREMappingFR1: ").append(pdschREMappingFR1);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

