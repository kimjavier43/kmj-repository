/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class MeasResult2NR implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private ARFCNValueNR ssbFrequency = null;
	private ARFCNValueNR refFreqCSIRS = null;
	private MeasResultNR measResultServingCell = null;
	private MeasResultListNR measResultNeighCellListNR = null;
	
	public MeasResult2NR() {
	}

	public MeasResult2NR(byte[] code) {
		this.code = code;
	}

	public void setSsbFrequency(ARFCNValueNR ssbFrequency) {
		this.ssbFrequency = ssbFrequency;
	}

	public ARFCNValueNR getSsbFrequency() {
		return ssbFrequency;
	}

	public void setRefFreqCSIRS(ARFCNValueNR refFreqCSIRS) {
		this.refFreqCSIRS = refFreqCSIRS;
	}

	public ARFCNValueNR getRefFreqCSIRS() {
		return refFreqCSIRS;
	}

	public void setMeasResultServingCell(MeasResultNR measResultServingCell) {
		this.measResultServingCell = measResultServingCell;
	}

	public MeasResultNR getMeasResultServingCell() {
		return measResultServingCell;
	}

	public void setMeasResultNeighCellListNR(MeasResultListNR measResultNeighCellListNR) {
		this.measResultNeighCellListNR = measResultNeighCellListNR;
	}

	public MeasResultListNR getMeasResultNeighCellListNR() {
		return measResultNeighCellListNR;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		codeLength += measResultNeighCellListNR.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, CONSTRUCTED, 3
		reverseOS.write(0xA3);
		codeLength += 1;
		
		if (measResultServingCell != null) {
			codeLength += measResultServingCell.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 2
			reverseOS.write(0xA2);
			codeLength += 1;
		}
		
		if (refFreqCSIRS != null) {
			codeLength += refFreqCSIRS.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			reverseOS.write(0x81);
			codeLength += 1;
		}
		
		if (ssbFrequency != null) {
			codeLength += ssbFrequency.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			ssbFrequency = new ARFCNValueNR();
			subCodeLength += ssbFrequency.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			refFreqCSIRS = new ARFCNValueNR();
			subCodeLength += refFreqCSIRS.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 2)) {
			measResultServingCell = new MeasResultNR();
			subCodeLength += measResultServingCell.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 3)) {
			measResultNeighCellListNR = new MeasResultListNR();
			subCodeLength += measResultNeighCellListNR.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (ssbFrequency != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ssbFrequency: ").append(ssbFrequency);
			firstSelectedElement = false;
		}
		
		if (refFreqCSIRS != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("refFreqCSIRS: ").append(refFreqCSIRS);
			firstSelectedElement = false;
		}
		
		if (measResultServingCell != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("measResultServingCell: ");
			measResultServingCell.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (!firstSelectedElement) {
			sb.append(",\n");
		}
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (measResultNeighCellListNR != null) {
			sb.append("measResultNeighCellListNR: ");
			measResultNeighCellListNR.appendAsString(sb, indentLevel + 1);
		}
		else {
			sb.append("measResultNeighCellListNR: <empty-required-field>");
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

