/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class ReconfigurationWithSync implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static class RachConfigDedicated implements BerType, Serializable {

		private static final long serialVersionUID = 1L;

		public byte[] code = null;
		private RACHConfigDedicated uplink = null;
		private RACHConfigDedicated supplementaryUplink = null;
		
		public RachConfigDedicated() {
		}

		public RachConfigDedicated(byte[] code) {
			this.code = code;
		}

		public void setUplink(RACHConfigDedicated uplink) {
			this.uplink = uplink;
		}

		public RACHConfigDedicated getUplink() {
			return uplink;
		}

		public void setSupplementaryUplink(RACHConfigDedicated supplementaryUplink) {
			this.supplementaryUplink = supplementaryUplink;
		}

		public RACHConfigDedicated getSupplementaryUplink() {
			return supplementaryUplink;
		}

		public int encode(OutputStream reverseOS) throws IOException {

			if (code != null) {
				for (int i = code.length - 1; i >= 0; i--) {
					reverseOS.write(code[i]);
				}
				return code.length;
			}

			int codeLength = 0;
			if (supplementaryUplink != null) {
				codeLength += supplementaryUplink.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
				reverseOS.write(0xA1);
				codeLength += 1;
				return codeLength;
			}
			
			if (uplink != null) {
				codeLength += uplink.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, CONSTRUCTED, 0
				reverseOS.write(0xA0);
				codeLength += 1;
				return codeLength;
			}
			
			throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
		}

		public int decode(InputStream is) throws IOException {
			return decode(is, null);
		}

		public int decode(InputStream is, BerTag berTag) throws IOException {

			int codeLength = 0;
			BerTag passedTag = berTag;

			if (berTag == null) {
				berTag = new BerTag();
				codeLength += berTag.decode(is);
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 0)) {
				uplink = new RACHConfigDedicated();
				codeLength += uplink.decode(is, false);
				return codeLength;
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
				supplementaryUplink = new RACHConfigDedicated();
				codeLength += supplementaryUplink.decode(is, false);
				return codeLength;
			}

			if (passedTag != null) {
				return 0;
			}

			throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
		}

		public void encodeAndSave(int encodingSizeGuess) throws IOException {
			ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
			encode(reverseOS);
			code = reverseOS.getArray();
		}

		public String toString() {
			StringBuilder sb = new StringBuilder();
			appendAsString(sb, 0);
			return sb.toString();
		}

		public void appendAsString(StringBuilder sb, int indentLevel) {

			if (uplink != null) {
				sb.append("uplink: ");
				uplink.appendAsString(sb, indentLevel + 1);
				return;
			}

			if (supplementaryUplink != null) {
				sb.append("supplementaryUplink: ");
				supplementaryUplink.appendAsString(sb, indentLevel + 1);
				return;
			}

			sb.append("<none>");
		}

	}

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private ServingCellConfigCommon spCellConfigCommon = null;
	private RNTIValue newUEIdentity = null;
	private BerEnum t304 = null;
	private RachConfigDedicated rachConfigDedicated = null;
	
	public ReconfigurationWithSync() {
	}

	public ReconfigurationWithSync(byte[] code) {
		this.code = code;
	}

	public void setSpCellConfigCommon(ServingCellConfigCommon spCellConfigCommon) {
		this.spCellConfigCommon = spCellConfigCommon;
	}

	public ServingCellConfigCommon getSpCellConfigCommon() {
		return spCellConfigCommon;
	}

	public void setNewUEIdentity(RNTIValue newUEIdentity) {
		this.newUEIdentity = newUEIdentity;
	}

	public RNTIValue getNewUEIdentity() {
		return newUEIdentity;
	}

	public void setT304(BerEnum t304) {
		this.t304 = t304;
	}

	public BerEnum getT304() {
		return t304;
	}

	public void setRachConfigDedicated(RachConfigDedicated rachConfigDedicated) {
		this.rachConfigDedicated = rachConfigDedicated;
	}

	public RachConfigDedicated getRachConfigDedicated() {
		return rachConfigDedicated;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		int sublength;

		if (rachConfigDedicated != null) {
			sublength = rachConfigDedicated.encode(reverseOS);
			codeLength += sublength;
			codeLength += BerLength.encodeLength(reverseOS, sublength);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 3
			reverseOS.write(0xA3);
			codeLength += 1;
		}
		
		codeLength += t304.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, PRIMITIVE, 2
		reverseOS.write(0x82);
		codeLength += 1;
		
		codeLength += newUEIdentity.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, PRIMITIVE, 1
		reverseOS.write(0x81);
		codeLength += 1;
		
		if (spCellConfigCommon != null) {
			codeLength += spCellConfigCommon.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 0
			reverseOS.write(0xA0);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 0)) {
			spCellConfigCommon = new ServingCellConfigCommon();
			subCodeLength += spCellConfigCommon.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			newUEIdentity = new RNTIValue();
			subCodeLength += newUEIdentity.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		else {
			throw new IOException("Tag does not match the mandatory sequence element tag.");
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			t304 = new BerEnum();
			subCodeLength += t304.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		else {
			throw new IOException("Tag does not match the mandatory sequence element tag.");
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 3)) {
			subCodeLength += length.decode(is);
			rachConfigDedicated = new RachConfigDedicated();
			subCodeLength += rachConfigDedicated.decode(is, null);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (spCellConfigCommon != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("spCellConfigCommon: ");
			spCellConfigCommon.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (!firstSelectedElement) {
			sb.append(",\n");
		}
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (newUEIdentity != null) {
			sb.append("newUEIdentity: ").append(newUEIdentity);
		}
		else {
			sb.append("newUEIdentity: <empty-required-field>");
		}
		
		sb.append(",\n");
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (t304 != null) {
			sb.append("t304: ").append(t304);
		}
		else {
			sb.append("t304: <empty-required-field>");
		}
		
		if (rachConfigDedicated != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("rachConfigDedicated: ");
			rachConfigDedicated.appendAsString(sb, indentLevel + 1);
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

