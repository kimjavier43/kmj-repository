/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class MeasParametersFRXDiff implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerEnum ssSINRMeas = null;
	private BerEnum csiRSRPAndRSRQMeasWithSSB = null;
	private BerEnum csiRSRPAndRSRQMeasWithoutSSB = null;
	private BerEnum csiSINRMeas = null;
	private BerEnum csiRSRLM = null;
	
	public MeasParametersFRXDiff() {
	}

	public MeasParametersFRXDiff(byte[] code) {
		this.code = code;
	}

	public void setSsSINRMeas(BerEnum ssSINRMeas) {
		this.ssSINRMeas = ssSINRMeas;
	}

	public BerEnum getSsSINRMeas() {
		return ssSINRMeas;
	}

	public void setCsiRSRPAndRSRQMeasWithSSB(BerEnum csiRSRPAndRSRQMeasWithSSB) {
		this.csiRSRPAndRSRQMeasWithSSB = csiRSRPAndRSRQMeasWithSSB;
	}

	public BerEnum getCsiRSRPAndRSRQMeasWithSSB() {
		return csiRSRPAndRSRQMeasWithSSB;
	}

	public void setCsiRSRPAndRSRQMeasWithoutSSB(BerEnum csiRSRPAndRSRQMeasWithoutSSB) {
		this.csiRSRPAndRSRQMeasWithoutSSB = csiRSRPAndRSRQMeasWithoutSSB;
	}

	public BerEnum getCsiRSRPAndRSRQMeasWithoutSSB() {
		return csiRSRPAndRSRQMeasWithoutSSB;
	}

	public void setCsiSINRMeas(BerEnum csiSINRMeas) {
		this.csiSINRMeas = csiSINRMeas;
	}

	public BerEnum getCsiSINRMeas() {
		return csiSINRMeas;
	}

	public void setCsiRSRLM(BerEnum csiRSRLM) {
		this.csiRSRLM = csiRSRLM;
	}

	public BerEnum getCsiRSRLM() {
		return csiRSRLM;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (csiRSRLM != null) {
			codeLength += csiRSRLM.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 4
			reverseOS.write(0x84);
			codeLength += 1;
		}
		
		if (csiSINRMeas != null) {
			codeLength += csiSINRMeas.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (csiRSRPAndRSRQMeasWithoutSSB != null) {
			codeLength += csiRSRPAndRSRQMeasWithoutSSB.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (csiRSRPAndRSRQMeasWithSSB != null) {
			codeLength += csiRSRPAndRSRQMeasWithSSB.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			reverseOS.write(0x81);
			codeLength += 1;
		}
		
		if (ssSINRMeas != null) {
			codeLength += ssSINRMeas.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			ssSINRMeas = new BerEnum();
			subCodeLength += ssSINRMeas.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			csiRSRPAndRSRQMeasWithSSB = new BerEnum();
			subCodeLength += csiRSRPAndRSRQMeasWithSSB.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			csiRSRPAndRSRQMeasWithoutSSB = new BerEnum();
			subCodeLength += csiRSRPAndRSRQMeasWithoutSSB.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			csiSINRMeas = new BerEnum();
			subCodeLength += csiSINRMeas.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 4)) {
			csiRSRLM = new BerEnum();
			subCodeLength += csiRSRLM.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (ssSINRMeas != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("ssSINRMeas: ").append(ssSINRMeas);
			firstSelectedElement = false;
		}
		
		if (csiRSRPAndRSRQMeasWithSSB != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("csiRSRPAndRSRQMeasWithSSB: ").append(csiRSRPAndRSRQMeasWithSSB);
			firstSelectedElement = false;
		}
		
		if (csiRSRPAndRSRQMeasWithoutSSB != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("csiRSRPAndRSRQMeasWithoutSSB: ").append(csiRSRPAndRSRQMeasWithoutSSB);
			firstSelectedElement = false;
		}
		
		if (csiSINRMeas != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("csiSINRMeas: ").append(csiSINRMeas);
			firstSelectedElement = false;
		}
		
		if (csiRSRLM != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("csiRSRLM: ").append(csiRSRLM);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

