/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class DRBToAddMod implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static class CnAssociation implements BerType, Serializable {

		private static final long serialVersionUID = 1L;

		public byte[] code = null;
		private BerInteger epsBearerIdentity = null;
		private SDAPConfig sdapConfig = null;
		
		public CnAssociation() {
		}

		public CnAssociation(byte[] code) {
			this.code = code;
		}

		public void setEpsBearerIdentity(BerInteger epsBearerIdentity) {
			this.epsBearerIdentity = epsBearerIdentity;
		}

		public BerInteger getEpsBearerIdentity() {
			return epsBearerIdentity;
		}

		public void setSdapConfig(SDAPConfig sdapConfig) {
			this.sdapConfig = sdapConfig;
		}

		public SDAPConfig getSdapConfig() {
			return sdapConfig;
		}

		public int encode(OutputStream reverseOS) throws IOException {

			if (code != null) {
				for (int i = code.length - 1; i >= 0; i--) {
					reverseOS.write(code[i]);
				}
				return code.length;
			}

			int codeLength = 0;
			if (sdapConfig != null) {
				codeLength += sdapConfig.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
				reverseOS.write(0xA1);
				codeLength += 1;
				return codeLength;
			}
			
			if (epsBearerIdentity != null) {
				codeLength += epsBearerIdentity.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 0
				reverseOS.write(0x80);
				codeLength += 1;
				return codeLength;
			}
			
			throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
		}

		public int decode(InputStream is) throws IOException {
			return decode(is, null);
		}

		public int decode(InputStream is, BerTag berTag) throws IOException {

			int codeLength = 0;
			BerTag passedTag = berTag;

			if (berTag == null) {
				berTag = new BerTag();
				codeLength += berTag.decode(is);
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
				epsBearerIdentity = new BerInteger();
				codeLength += epsBearerIdentity.decode(is, false);
				return codeLength;
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
				sdapConfig = new SDAPConfig();
				codeLength += sdapConfig.decode(is, false);
				return codeLength;
			}

			if (passedTag != null) {
				return 0;
			}

			throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
		}

		public void encodeAndSave(int encodingSizeGuess) throws IOException {
			ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
			encode(reverseOS);
			code = reverseOS.getArray();
		}

		public String toString() {
			StringBuilder sb = new StringBuilder();
			appendAsString(sb, 0);
			return sb.toString();
		}

		public void appendAsString(StringBuilder sb, int indentLevel) {

			if (epsBearerIdentity != null) {
				sb.append("epsBearerIdentity: ").append(epsBearerIdentity);
				return;
			}

			if (sdapConfig != null) {
				sb.append("sdapConfig: ");
				sdapConfig.appendAsString(sb, indentLevel + 1);
				return;
			}

			sb.append("<none>");
		}

	}

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private CnAssociation cnAssociation = null;
	private DRBIdentity drbIdentity = null;
	private BerEnum reestablishPDCP = null;
	private BerEnum recoverPDCP = null;
	private PDCPConfig pdcpConfig = null;
	
	public DRBToAddMod() {
	}

	public DRBToAddMod(byte[] code) {
		this.code = code;
	}

	public void setCnAssociation(CnAssociation cnAssociation) {
		this.cnAssociation = cnAssociation;
	}

	public CnAssociation getCnAssociation() {
		return cnAssociation;
	}

	public void setDrbIdentity(DRBIdentity drbIdentity) {
		this.drbIdentity = drbIdentity;
	}

	public DRBIdentity getDrbIdentity() {
		return drbIdentity;
	}

	public void setReestablishPDCP(BerEnum reestablishPDCP) {
		this.reestablishPDCP = reestablishPDCP;
	}

	public BerEnum getReestablishPDCP() {
		return reestablishPDCP;
	}

	public void setRecoverPDCP(BerEnum recoverPDCP) {
		this.recoverPDCP = recoverPDCP;
	}

	public BerEnum getRecoverPDCP() {
		return recoverPDCP;
	}

	public void setPdcpConfig(PDCPConfig pdcpConfig) {
		this.pdcpConfig = pdcpConfig;
	}

	public PDCPConfig getPdcpConfig() {
		return pdcpConfig;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		int sublength;

		if (pdcpConfig != null) {
			codeLength += pdcpConfig.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 4
			reverseOS.write(0xA4);
			codeLength += 1;
		}
		
		if (recoverPDCP != null) {
			codeLength += recoverPDCP.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (reestablishPDCP != null) {
			codeLength += reestablishPDCP.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		codeLength += drbIdentity.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, PRIMITIVE, 1
		reverseOS.write(0x81);
		codeLength += 1;
		
		if (cnAssociation != null) {
			sublength = cnAssociation.encode(reverseOS);
			codeLength += sublength;
			codeLength += BerLength.encodeLength(reverseOS, sublength);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 0
			reverseOS.write(0xA0);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 0)) {
			subCodeLength += length.decode(is);
			cnAssociation = new CnAssociation();
			subCodeLength += cnAssociation.decode(is, null);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			drbIdentity = new DRBIdentity();
			subCodeLength += drbIdentity.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		else {
			throw new IOException("Tag does not match the mandatory sequence element tag.");
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			reestablishPDCP = new BerEnum();
			subCodeLength += reestablishPDCP.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			recoverPDCP = new BerEnum();
			subCodeLength += recoverPDCP.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 4)) {
			pdcpConfig = new PDCPConfig();
			subCodeLength += pdcpConfig.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (cnAssociation != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("cnAssociation: ");
			cnAssociation.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (!firstSelectedElement) {
			sb.append(",\n");
		}
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (drbIdentity != null) {
			sb.append("drbIdentity: ").append(drbIdentity);
		}
		else {
			sb.append("drbIdentity: <empty-required-field>");
		}
		
		if (reestablishPDCP != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("reestablishPDCP: ").append(reestablishPDCP);
		}
		
		if (recoverPDCP != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("recoverPDCP: ").append(recoverPDCP);
		}
		
		if (pdcpConfig != null) {
			sb.append(",\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("pdcpConfig: ");
			pdcpConfig.appendAsString(sb, indentLevel + 1);
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

