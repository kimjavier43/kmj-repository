/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class PUSCHConfigCommon implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerEnum groupHoppingEnabledTransformPrecoding = null;
	private PUSCHTimeDomainResourceAllocationList puschTimeDomainAllocationList = null;
	private BerInteger msg3DeltaPreamble = null;
	private BerInteger p0NominalWithGrant = null;
	
	public PUSCHConfigCommon() {
	}

	public PUSCHConfigCommon(byte[] code) {
		this.code = code;
	}

	public void setGroupHoppingEnabledTransformPrecoding(BerEnum groupHoppingEnabledTransformPrecoding) {
		this.groupHoppingEnabledTransformPrecoding = groupHoppingEnabledTransformPrecoding;
	}

	public BerEnum getGroupHoppingEnabledTransformPrecoding() {
		return groupHoppingEnabledTransformPrecoding;
	}

	public void setPuschTimeDomainAllocationList(PUSCHTimeDomainResourceAllocationList puschTimeDomainAllocationList) {
		this.puschTimeDomainAllocationList = puschTimeDomainAllocationList;
	}

	public PUSCHTimeDomainResourceAllocationList getPuschTimeDomainAllocationList() {
		return puschTimeDomainAllocationList;
	}

	public void setMsg3DeltaPreamble(BerInteger msg3DeltaPreamble) {
		this.msg3DeltaPreamble = msg3DeltaPreamble;
	}

	public BerInteger getMsg3DeltaPreamble() {
		return msg3DeltaPreamble;
	}

	public void setP0NominalWithGrant(BerInteger p0NominalWithGrant) {
		this.p0NominalWithGrant = p0NominalWithGrant;
	}

	public BerInteger getP0NominalWithGrant() {
		return p0NominalWithGrant;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (p0NominalWithGrant != null) {
			codeLength += p0NominalWithGrant.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (msg3DeltaPreamble != null) {
			codeLength += msg3DeltaPreamble.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (puschTimeDomainAllocationList != null) {
			codeLength += puschTimeDomainAllocationList.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
			reverseOS.write(0xA1);
			codeLength += 1;
		}
		
		if (groupHoppingEnabledTransformPrecoding != null) {
			codeLength += groupHoppingEnabledTransformPrecoding.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			groupHoppingEnabledTransformPrecoding = new BerEnum();
			subCodeLength += groupHoppingEnabledTransformPrecoding.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
			puschTimeDomainAllocationList = new PUSCHTimeDomainResourceAllocationList();
			subCodeLength += puschTimeDomainAllocationList.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			msg3DeltaPreamble = new BerInteger();
			subCodeLength += msg3DeltaPreamble.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			p0NominalWithGrant = new BerInteger();
			subCodeLength += p0NominalWithGrant.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (groupHoppingEnabledTransformPrecoding != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("groupHoppingEnabledTransformPrecoding: ").append(groupHoppingEnabledTransformPrecoding);
			firstSelectedElement = false;
		}
		
		if (puschTimeDomainAllocationList != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("puschTimeDomainAllocationList: ");
			puschTimeDomainAllocationList.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (msg3DeltaPreamble != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("msg3DeltaPreamble: ").append(msg3DeltaPreamble);
			firstSelectedElement = false;
		}
		
		if (p0NominalWithGrant != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("p0NominalWithGrant: ").append(p0NominalWithGrant);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

