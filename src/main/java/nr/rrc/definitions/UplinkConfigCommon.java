/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class UplinkConfigCommon implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private FrequencyInfoUL frequencyInfoUL = null;
	private BWPUplinkCommon initialUplinkBWP = null;
	private TimeAlignmentTimer timeAlignmentTimerCommon = null;
	
	public UplinkConfigCommon() {
	}

	public UplinkConfigCommon(byte[] code) {
		this.code = code;
	}

	public void setFrequencyInfoUL(FrequencyInfoUL frequencyInfoUL) {
		this.frequencyInfoUL = frequencyInfoUL;
	}

	public FrequencyInfoUL getFrequencyInfoUL() {
		return frequencyInfoUL;
	}

	public void setInitialUplinkBWP(BWPUplinkCommon initialUplinkBWP) {
		this.initialUplinkBWP = initialUplinkBWP;
	}

	public BWPUplinkCommon getInitialUplinkBWP() {
		return initialUplinkBWP;
	}

	public void setTimeAlignmentTimerCommon(TimeAlignmentTimer timeAlignmentTimerCommon) {
		this.timeAlignmentTimerCommon = timeAlignmentTimerCommon;
	}

	public TimeAlignmentTimer getTimeAlignmentTimerCommon() {
		return timeAlignmentTimerCommon;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		codeLength += timeAlignmentTimerCommon.encode(reverseOS, false);
		// write tag: CONTEXT_CLASS, PRIMITIVE, 2
		reverseOS.write(0x82);
		codeLength += 1;
		
		if (initialUplinkBWP != null) {
			codeLength += initialUplinkBWP.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
			reverseOS.write(0xA1);
			codeLength += 1;
		}
		
		if (frequencyInfoUL != null) {
			codeLength += frequencyInfoUL.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 0
			reverseOS.write(0xA0);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 0)) {
			frequencyInfoUL = new FrequencyInfoUL();
			subCodeLength += frequencyInfoUL.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
			initialUplinkBWP = new BWPUplinkCommon();
			subCodeLength += initialUplinkBWP.decode(is, false);
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			timeAlignmentTimerCommon = new TimeAlignmentTimer();
			subCodeLength += timeAlignmentTimerCommon.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (frequencyInfoUL != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("frequencyInfoUL: ");
			frequencyInfoUL.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (initialUplinkBWP != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("initialUplinkBWP: ");
			initialUplinkBWP.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (!firstSelectedElement) {
			sb.append(",\n");
		}
		for (int i = 0; i < indentLevel + 1; i++) {
			sb.append("\t");
		}
		if (timeAlignmentTimerCommon != null) {
			sb.append("timeAlignmentTimerCommon: ").append(timeAlignmentTimerCommon);
		}
		else {
			sb.append("timeAlignmentTimerCommon: <empty-required-field>");
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

