/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.rrc.definitions;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;


public class MACParametersXDDDiff implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private BerEnum skipUplinkTxDynamic = null;
	private BerEnum logicalChannelSRDelayTimer = null;
	private BerEnum longDRXCycle = null;
	private BerEnum shortDRXCycle = null;
	private BerEnum multipleSRConfigurations = null;
	private BerEnum multipleConfiguredGrants = null;
	
	public MACParametersXDDDiff() {
	}

	public MACParametersXDDDiff(byte[] code) {
		this.code = code;
	}

	public void setSkipUplinkTxDynamic(BerEnum skipUplinkTxDynamic) {
		this.skipUplinkTxDynamic = skipUplinkTxDynamic;
	}

	public BerEnum getSkipUplinkTxDynamic() {
		return skipUplinkTxDynamic;
	}

	public void setLogicalChannelSRDelayTimer(BerEnum logicalChannelSRDelayTimer) {
		this.logicalChannelSRDelayTimer = logicalChannelSRDelayTimer;
	}

	public BerEnum getLogicalChannelSRDelayTimer() {
		return logicalChannelSRDelayTimer;
	}

	public void setLongDRXCycle(BerEnum longDRXCycle) {
		this.longDRXCycle = longDRXCycle;
	}

	public BerEnum getLongDRXCycle() {
		return longDRXCycle;
	}

	public void setShortDRXCycle(BerEnum shortDRXCycle) {
		this.shortDRXCycle = shortDRXCycle;
	}

	public BerEnum getShortDRXCycle() {
		return shortDRXCycle;
	}

	public void setMultipleSRConfigurations(BerEnum multipleSRConfigurations) {
		this.multipleSRConfigurations = multipleSRConfigurations;
	}

	public BerEnum getMultipleSRConfigurations() {
		return multipleSRConfigurations;
	}

	public void setMultipleConfiguredGrants(BerEnum multipleConfiguredGrants) {
		this.multipleConfiguredGrants = multipleConfiguredGrants;
	}

	public BerEnum getMultipleConfiguredGrants() {
		return multipleConfiguredGrants;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		if (multipleConfiguredGrants != null) {
			codeLength += multipleConfiguredGrants.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 5
			reverseOS.write(0x85);
			codeLength += 1;
		}
		
		if (multipleSRConfigurations != null) {
			codeLength += multipleSRConfigurations.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 4
			reverseOS.write(0x84);
			codeLength += 1;
		}
		
		if (shortDRXCycle != null) {
			codeLength += shortDRXCycle.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 3
			reverseOS.write(0x83);
			codeLength += 1;
		}
		
		if (longDRXCycle != null) {
			codeLength += longDRXCycle.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 2
			reverseOS.write(0x82);
			codeLength += 1;
		}
		
		if (logicalChannelSRDelayTimer != null) {
			codeLength += logicalChannelSRDelayTimer.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 1
			reverseOS.write(0x81);
			codeLength += 1;
		}
		
		if (skipUplinkTxDynamic != null) {
			codeLength += skipUplinkTxDynamic.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, PRIMITIVE, 0
			reverseOS.write(0x80);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
			skipUplinkTxDynamic = new BerEnum();
			subCodeLength += skipUplinkTxDynamic.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
			logicalChannelSRDelayTimer = new BerEnum();
			subCodeLength += logicalChannelSRDelayTimer.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 2)) {
			longDRXCycle = new BerEnum();
			subCodeLength += longDRXCycle.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 3)) {
			shortDRXCycle = new BerEnum();
			subCodeLength += shortDRXCycle.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 4)) {
			multipleSRConfigurations = new BerEnum();
			subCodeLength += multipleSRConfigurations.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 5)) {
			multipleConfiguredGrants = new BerEnum();
			subCodeLength += multipleConfiguredGrants.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (skipUplinkTxDynamic != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("skipUplinkTxDynamic: ").append(skipUplinkTxDynamic);
			firstSelectedElement = false;
		}
		
		if (logicalChannelSRDelayTimer != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("logicalChannelSRDelayTimer: ").append(logicalChannelSRDelayTimer);
			firstSelectedElement = false;
		}
		
		if (longDRXCycle != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("longDRXCycle: ").append(longDRXCycle);
			firstSelectedElement = false;
		}
		
		if (shortDRXCycle != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("shortDRXCycle: ").append(shortDRXCycle);
			firstSelectedElement = false;
		}
		
		if (multipleSRConfigurations != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("multipleSRConfigurations: ").append(multipleSRConfigurations);
			firstSelectedElement = false;
		}
		
		if (multipleConfiguredGrants != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("multipleConfiguredGrants: ").append(multipleConfiguredGrants);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

