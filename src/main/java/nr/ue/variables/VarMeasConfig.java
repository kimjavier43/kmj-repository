/**
 * This class file was automatically generated by jASN1 v1.10.0 (http://www.openmuc.org)
 */

package nr.ue.variables;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import org.openmuc.jasn1.ber.*;
import org.openmuc.jasn1.ber.types.*;
import org.openmuc.jasn1.ber.types.string.*;

import nr.rrc.definitions.MeasId;
import nr.rrc.definitions.MeasIdToAddModList;
import nr.rrc.definitions.MeasObjectToAddModList;
import nr.rrc.definitions.PhysCellId;
import nr.rrc.definitions.QuantityConfig;
import nr.rrc.definitions.RSRPRange;
import nr.rrc.definitions.ReportConfigToAddModList;

public class VarMeasConfig implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	public static class SMeasureConfig implements BerType, Serializable {

		private static final long serialVersionUID = 1L;

		public byte[] code = null;
		private RSRPRange ssbRSRP = null;
		private RSRPRange csiRSRP = null;
		
		public SMeasureConfig() {
		}

		public SMeasureConfig(byte[] code) {
			this.code = code;
		}

		public void setSsbRSRP(RSRPRange ssbRSRP) {
			this.ssbRSRP = ssbRSRP;
		}

		public RSRPRange getSsbRSRP() {
			return ssbRSRP;
		}

		public void setCsiRSRP(RSRPRange csiRSRP) {
			this.csiRSRP = csiRSRP;
		}

		public RSRPRange getCsiRSRP() {
			return csiRSRP;
		}

		public int encode(OutputStream reverseOS) throws IOException {

			if (code != null) {
				for (int i = code.length - 1; i >= 0; i--) {
					reverseOS.write(code[i]);
				}
				return code.length;
			}

			int codeLength = 0;
			if (csiRSRP != null) {
				codeLength += csiRSRP.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 1
				reverseOS.write(0x81);
				codeLength += 1;
				return codeLength;
			}
			
			if (ssbRSRP != null) {
				codeLength += ssbRSRP.encode(reverseOS, false);
				// write tag: CONTEXT_CLASS, PRIMITIVE, 0
				reverseOS.write(0x80);
				codeLength += 1;
				return codeLength;
			}
			
			throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
		}

		public int decode(InputStream is) throws IOException {
			return decode(is, null);
		}

		public int decode(InputStream is, BerTag berTag) throws IOException {

			int codeLength = 0;
			BerTag passedTag = berTag;

			if (berTag == null) {
				berTag = new BerTag();
				codeLength += berTag.decode(is);
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 0)) {
				ssbRSRP = new RSRPRange();
				codeLength += ssbRSRP.decode(is, false);
				return codeLength;
			}

			if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.PRIMITIVE, 1)) {
				csiRSRP = new RSRPRange();
				codeLength += csiRSRP.decode(is, false);
				return codeLength;
			}

			if (passedTag != null) {
				return 0;
			}

			throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
		}

		public void encodeAndSave(int encodingSizeGuess) throws IOException {
			ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
			encode(reverseOS);
			code = reverseOS.getArray();
		}

		public String toString() {
			StringBuilder sb = new StringBuilder();
			appendAsString(sb, 0);
			return sb.toString();
		}

		public void appendAsString(StringBuilder sb, int indentLevel) {

			if (ssbRSRP != null) {
				sb.append("ssbRSRP: ").append(ssbRSRP);
				return;
			}

			if (csiRSRP != null) {
				sb.append("csiRSRP: ").append(csiRSRP);
				return;
			}

			sb.append("<none>");
		}

	}

	public static final BerTag tag = new BerTag(BerTag.UNIVERSAL_CLASS, BerTag.CONSTRUCTED, 16);

	public byte[] code = null;
	private MeasIdToAddModList measIdList = null;
	private MeasObjectToAddModList measObjectList = null;
	private ReportConfigToAddModList reportConfigList = null;
	private QuantityConfig quantityConfig = null;
	private SMeasureConfig sMeasureConfig = null;
	
	public VarMeasConfig() {
	}

	public VarMeasConfig(byte[] code) {
		this.code = code;
	}

	public void setMeasIdList(MeasIdToAddModList measIdList) {
		this.measIdList = measIdList;
	}

	public MeasIdToAddModList getMeasIdList() {
		return measIdList;
	}

	public void setMeasObjectList(MeasObjectToAddModList measObjectList) {
		this.measObjectList = measObjectList;
	}

	public MeasObjectToAddModList getMeasObjectList() {
		return measObjectList;
	}

	public void setReportConfigList(ReportConfigToAddModList reportConfigList) {
		this.reportConfigList = reportConfigList;
	}

	public ReportConfigToAddModList getReportConfigList() {
		return reportConfigList;
	}

	public void setQuantityConfig(QuantityConfig quantityConfig) {
		this.quantityConfig = quantityConfig;
	}

	public QuantityConfig getQuantityConfig() {
		return quantityConfig;
	}

	public void setSMeasureConfig(SMeasureConfig sMeasureConfig) {
		this.sMeasureConfig = sMeasureConfig;
	}

	public SMeasureConfig getSMeasureConfig() {
		return sMeasureConfig;
	}

	public int encode(OutputStream reverseOS) throws IOException {
		return encode(reverseOS, true);
	}

	public int encode(OutputStream reverseOS, boolean withTag) throws IOException {

		if (code != null) {
			for (int i = code.length - 1; i >= 0; i--) {
				reverseOS.write(code[i]);
			}
			if (withTag) {
				return tag.encode(reverseOS) + code.length;
			}
			return code.length;
		}

		int codeLength = 0;
		int sublength;

		if (sMeasureConfig != null) {
			sublength = sMeasureConfig.encode(reverseOS);
			codeLength += sublength;
			codeLength += BerLength.encodeLength(reverseOS, sublength);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 4
			reverseOS.write(0xA4);
			codeLength += 1;
		}
		
		if (quantityConfig != null) {
			codeLength += quantityConfig.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 3
			reverseOS.write(0xA3);
			codeLength += 1;
		}
		
		if (reportConfigList != null) {
			codeLength += reportConfigList.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 2
			reverseOS.write(0xA2);
			codeLength += 1;
		}
		
		if (measObjectList != null) {
			codeLength += measObjectList.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 1
			reverseOS.write(0xA1);
			codeLength += 1;
		}
		
		if (measIdList != null) {
			codeLength += measIdList.encode(reverseOS, false);
			// write tag: CONTEXT_CLASS, CONSTRUCTED, 0
			reverseOS.write(0xA0);
			codeLength += 1;
		}
		
		codeLength += BerLength.encodeLength(reverseOS, codeLength);

		if (withTag) {
			codeLength += tag.encode(reverseOS);
		}

		return codeLength;

	}

	public int decode(InputStream is) throws IOException {
		return decode(is, true);
	}

	public int decode(InputStream is, boolean withTag) throws IOException {
		int codeLength = 0;
		int subCodeLength = 0;
		BerTag berTag = new BerTag();

		if (withTag) {
			codeLength += tag.decodeAndCheck(is);
		}

		BerLength length = new BerLength();
		codeLength += length.decode(is);

		int totalLength = length.val;
		codeLength += totalLength;

		if (totalLength == 0) {
			return codeLength;
		}
		subCodeLength += berTag.decode(is);
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 0)) {
			measIdList = new MeasIdToAddModList();
			subCodeLength += measIdList.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 1)) {
			measObjectList = new MeasObjectToAddModList();
			subCodeLength += measObjectList.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 2)) {
			reportConfigList = new ReportConfigToAddModList();
			subCodeLength += reportConfigList.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 3)) {
			quantityConfig = new QuantityConfig();
			subCodeLength += quantityConfig.decode(is, false);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
			subCodeLength += berTag.decode(is);
		}
		
		if (berTag.equals(BerTag.CONTEXT_CLASS, BerTag.CONSTRUCTED, 4)) {
			subCodeLength += length.decode(is);
			sMeasureConfig = new SMeasureConfig();
			subCodeLength += sMeasureConfig.decode(is, null);
			if (subCodeLength == totalLength) {
				return codeLength;
			}
		}
		throw new IOException("Unexpected end of sequence, length tag: " + totalLength + ", actual sequence length: " + subCodeLength);

		
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS, false);
		code = reverseOS.getArray();
	}

	public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		sb.append("{");
		boolean firstSelectedElement = true;
		if (measIdList != null) {
			sb.append("\n");
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("measIdList: ");
			measIdList.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (measObjectList != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("measObjectList: ");
			measObjectList.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (reportConfigList != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("reportConfigList: ");
			reportConfigList.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (quantityConfig != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("quantityConfig: ");
			quantityConfig.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		if (sMeasureConfig != null) {
			if (!firstSelectedElement) {
				sb.append(",\n");
			}
			for (int i = 0; i < indentLevel + 1; i++) {
				sb.append("\t");
			}
			sb.append("sMeasureConfig: ");
			sMeasureConfig.appendAsString(sb, indentLevel + 1);
			firstSelectedElement = false;
		}
		
		sb.append("\n");
		for (int i = 0; i < indentLevel; i++) {
			sb.append("\t");
		}
		sb.append("}");
	}

}

